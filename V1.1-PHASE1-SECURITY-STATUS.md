# Provatiloanbd v1.1 — Phase 1 Security Status

## Baseline
- `main` is the live v1.0 baseline.
- `v1.0-stable` is the recovery branch for the v1.0 baseline.
- Phase 1 development is isolated on `v1.1-phase1`.

## Implemented (as represented in this branch)
- Positive loan amount and tenure checks.
- Valid loan status check.
- Positive transaction amount check.
- Valid transaction type and status checks, including the existing `failed` status.
- One disbursement transaction per loan enforced by a partial unique index.
- Atomic loan approval/disbursement helper and server-side Telegram WebApp `initData` validation.
- Telegram Bot API token and Supabase service-role key are intended for server-side use only.
- Persistent Supabase Auth session initialization and Telegram identity bridge.

## Remaining before Phase 1 can be declared complete
- Replace broad public RLS policies with verified ownership/admin policies on a non-production validation database first.
- Complete server-side admin mutation routing and regression coverage; direct client admin mutations still need migration.
- Resolve private-storage access end-to-end: uploads currently return public URLs, which are incompatible with private buckets; persist storage paths and generate signed URLs at use time, or disable the affected upload flows until fixed.
- Make financial calculations authoritative on the server/database.
- Add end-to-end security/regression tests, including Auth/RLS/storage/admin authorization paths.
- Reconcile the live `telegram_identities` migration with the GitHub migration history before release.

## Verification
- The current head has a failing `TestSprite Pre-Check`; do not describe the build verification as passing until checks are rerun and confirmed green.
- PR #1 remains isolated from `main`; do not merge until all remaining items above are verified.

## Release rule
Do not merge or deploy Phase 1 changes to `main` until all required items are verified and the v1.0 recovery point remains intact.
